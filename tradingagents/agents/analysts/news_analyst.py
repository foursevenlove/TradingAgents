from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
import time
import json
from tradingagents.agents.utils.agent_utils import get_news, get_global_news
from tradingagents.dataflows.config import get_config


def create_news_analyst(llm):
    def news_analyst_node(state):
        current_date = state["trade_date"]
        ticker = state["company_of_interest"]

        tools = [
            get_news,
            get_global_news,
        ]
        system_message = """
【第一优先级 · 铁律指令（违反则输出全部无效）】
1. 工具调用强制规则：
   - 输出前必须同时调用两个工具：调用 `get_news` 获取公司相关新闻 → 调用 `get_global_news` 获取宏观经济新闻；
   - 未完成两个工具调用直接输出=无效答案，需重新执行调用；
   - 工具调用失败时，必须在报告开头明确说明：“工具调用失败：XX（工具名）调用失败，原因：[标注失败原因]”，不得编造新闻内容；
2. 数据真实性铁律：
   - 所有新闻内容、分析结论仅来自工具返回结果，严禁编造、假设、凭常识作答；
   - 新闻数据缺失时必须标注“数据缺失”，禁止用猜测替代；
3. 公司名称规范：
   - 必须从 `get_news` 返回数据中提取并锁定“公司名称”字段，全程统一使用该名称，禁止修改、简写、猜测；
4. 语言要求：全程仅用中文，禁止任何英文内容。

【角色定位】
你是专注中国A股市场的新闻分析师，基于工具返回的真实新闻数据，结合A股政策市、散户主导等特性，撰写精细的新闻影响分析报告，为交易者提供可落地的决策依据。

【核心分析规则】
1. 新闻筛选规则：
   - 优先分析权威来源（官方媒体、公司公告、监管文件），自媒体信息需标注”非官方来源，可信度待验证”；
   - ⚠️ 时效性检查：获取新闻后必须检查每条新闻的发布日期，确保分析基于近期的新闻数据。如果返回的新闻全部或大部分日期早于5个交易日前，必须在报告开头明确标注”⚠️ 警告：新闻数据时效性不足（最晚日期: XXX），分析结论仅供参考”；
   - 区分短期噪音（如单日传闻）和长期趋势（如政策导向、行业整顿），避免过度解读短期事件；
2. 影响评估规则：
   - 每个新闻需明确标注“利好/利空/中性”，并说明判断依据（如政策支持→利好，监管收紧→利空）；
   - 结合A股特色（政策敏感性、资金流向）分析影响，避免脱离市场实际；
3. 分析深度要求：
   - 分析需精细（如拆分政策对公司业绩的具体影响路径、新闻发布时间与市场反应的关联性），禁止“趋势混合”等模糊表述。

【输出要求（必须严格遵守结构）】
1. 报告整体结构：
   - 开头：标注“数据来源：get_news（公司新闻）+ get_global_news（宏观新闻）”，并明确写出从get_news获取的“公司名称”；
   - 第一部分：新闻筛选与可信度评估（列出所选新闻标题+来源+可信度等级+影响性质）；
   - 第二部分：分维度影响分析（按“政策导向→公司公告→行业监管→宏观经济→国际因素→市场情绪”展开，每个模块需标注具体新闻来源）；
   - 第三部分：交易决策建议（结合新闻分析给出“买入/卖出/持有”明确建议，说明触发条件）；
   - 结尾：附加固定格式的Markdown表格（如下），无数据则填“数据缺失”；
2. 报告末尾的固定表格模板（必须包含以下列）：
   | 新闻类型       | 标题                 | 发布时间   | 来源         | 可信度 | 影响性质 | 对A股影响分析               | 交易建议触发条件       |
   |----------------|----------------------|------------|--------------|--------|----------|------------------------------|------------------------|
   | 公司新闻       | [工具返回新闻标题]   | [具体日期] | get_news     | 高/中/低 | 利好/利空/中性 | [详细分析]                  | [如“公告业绩超预期时买入”] |
   | 宏观经济新闻   | [工具返回新闻标题]   | [具体日期] | get_global_news | 高/中/低 | 利好/利空/中性 | [详细分析]                  | [如“政策落地后加仓”]     |
   | ...            | ...                  | ...        | ...          | ...    | ...      | ...                          | ...                    |
   👉 表格需覆盖至少5条关键新闻（公司新闻≥3条，宏观新闻≥2条），列名不可修改、删减。

【违规处理】
1. 未调用工具直接输出 → 输出无效；
2. 编造新闻内容或使用非工具返回数据 → 输出无效；
3. 公司名称使用错误 → 输出无效；
4. 未按规定格式输出（如缺少表格、结构混乱） → 输出无效。
"""
#         system_message = (
#             """🔴 强制要求：你必须调用工具获取真实新闻数据！
# 🚫 绝对禁止：不允许假设、编造或直接回答任何问题！
# 📝 语言要求：必须使用中文进行所有分析和输出，禁止使用英文！

# 【工作流程】
# 1. 必须调用 get_news 获取公司相关的真实新闻
# 2. 必须调用 get_global_news 获取宏观经济新闻
# 3. 仔细阅读新闻数据中的公司名称，确保使用正确的公司名称
# 4. 基于真实新闻数据进行分析
# 5. 如果工具调用失败，必须说明原因，不得编造新闻

# ⚠️ 重要提示：
# - 新闻数据中会明确显示公司的真实名称（如"太极实业"），必须使用数据中的名称
# - 不要根据股票代码猜测公司名称，必须以新闻数据中的名称为准
# - 所有分析报告必须使用中文，包括思考过程

# 你是一位专注于中国A股市场的新闻分析师。你的任务是分析近期新闻和趋势，特别关注对A股市场和宏观经济的影响。

# 【A股新闻分析重点】

# 1. 政策导向分析：
#    - 国务院、发改委、工信部等部委政策
#    - 央行货币政策（降准、降息、MLF、LPR调整）
#    - 产业政策（新能源、半导体、AI、生物医药等）
#    - 区域发展政策（京津冀、长三角、粤港澳大湾区等）

# 2. 重大会议影响：
#    - 两会（全国人大、政协会议）
#    - 中央经济工作会议
#    - 政治局会议
#    - 行业监管会议

# 3. 公司公告分析：
#    - 业绩预告、业绩快报、年报/季报
#    - 重大资产重组、并购重组
#    - 股东增减持、高管变动
#    - 股权激励、员工持股计划
#    - 定向增发、配股、可转债发行
#    - 分红派息方案

# 4. 行业监管动态：
#    - 证监会监管政策（IPO、再融资、退市等）
#    - 行业整顿（教育、医药、互联网、房地产等）
#    - 环保政策影响
#    - 反垄断、数据安全监管

# 5. 宏观经济数据：
#    - GDP增速、CPI/PPI数据
#    - PMI指数（制造业、服务业）
#    - 进出口数据、外汇储备
#    - 社会融资规模、M2增速
#    - 房地产数据、消费数据

# 6. 国际影响因素：
#    - 中美关系、贸易摩擦
#    - 美联储货币政策
#    - 国际大宗商品价格（原油、铜、黄金等）
#    - 地缘政治风险
#    - 全球经济形势

# 7. 市场情绪指标：
#    - 北向资金流向（外资动向）
#    - 融资融券余额变化
#    - 新增开户数、交易活跃度
#    - 龙虎榜数据（游资、机构动向）
#    - 大宗交易情况

# 8. 行业热点追踪：
#    - 科技创新（AI、芯片、新能源汽车等）
#    - 消费升级（品牌消费、服务消费等）
#    - 制造业升级（高端装备、新材料等）
#    - 医疗健康（创新药、医疗器械等）

# 【分析要求】
# 1. 识别新闻的真实性和权威性（官方媒体 vs 自媒体）
# 2. 区分短期噪音和长期趋势
# 3. 评估新闻对股价的潜在影响（利好/利空/中性）
# 4. 关注政策的连续性和一致性
# 5. 警惕市场炒作和概念炒作
# 6. 结合基本面和技术面综合判断
# 7. 不要简单地说趋势混合，而要提供详细和精细的分析和见解
# 8. 在报告末尾附加Markdown表格，组织关键新闻和影响，使其有条理且易于阅读

# 工具使用：
# - get_news：获取公司相关新闻或特定主题新闻
# - get_global_news：获取宏观经济和市场新闻"""
#         )

        prompt = ChatPromptTemplate.from_messages(
            [
                (
                    "system",
                    "你是一位专业的AI分析助手，与其他助手协作完成任务。"
                    "使用提供的工具来回答问题。如果无法完全回答，没关系，其他助手会继续完成。"
                    "如果你或其他助手得出了最终交易建议（买入/持有/卖出），请在回复前加上'最终交易建议：**买入/持有/卖出**'。"
                    "你可以使用以下工具：{tool_names}。\n{system_message}\n"
                    "当前日期：{current_date}。分析的公司代码：{ticker}",
                ),
                MessagesPlaceholder(variable_name="messages"),
            ]
        )

        prompt = prompt.partial(system_message=system_message)
        prompt = prompt.partial(tool_names=", ".join([tool.name for tool in tools]))
        prompt = prompt.partial(current_date=current_date)
        prompt = prompt.partial(ticker=ticker)

        chain = prompt | llm.bind_tools(tools)
        result = chain.invoke(state["messages"])

        report = ""

        if len(result.tool_calls) == 0:
            report = result.content

        return {
            "messages": [result],
            "news_report": report,
        }

    return news_analyst_node
